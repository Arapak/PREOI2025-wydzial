#include "wydlib.h"

#include <bits/stdc++.h>

using namespace std;

std::vector<std::vector<int>> Bitek(int n, std::array<int, K> A, std::array<int, K> B) {
    return vector<vector<int>>(n, vector<int>(n, 1));
}

std::array<int, K> Bajtazar(std::array<int, 9> A) { return array<int, K>{0}; }
